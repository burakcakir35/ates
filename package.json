{
  "name": "ates",
  "version": "0.1.0",
  "private": true,
  "description": "TXID-based provably-fair betting game — play-money prototype",
  "packageManager": "pnpm@9.15.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --parallel --filter @ates/server --filter @ates/web dev"
  },
  "engines": {
    "node": ">=20"
  }
}
